const AuthReducer = (state: any, action: any) => {
    switch (action.type) {
        case 'user':
            return{
                // state.user = action.payload
            }
        case 'user':
            return{
                // state.loading = true
            }
        case 'user':
            return{
                // state.user = action.payload
            }
        default:
            break;
    }
}

